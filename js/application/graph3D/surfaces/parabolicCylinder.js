// параболический цилиндр
Surface.prototype.parabolicCylinder = (x = 0, y = 0, z = 0, pointCount = 20) => {
    const points = [];
    const edges = [];
    const polygones = [];

    function createPoints() {              
    }

    function createEdges() {
    }

    function createPolygons() {
    }

    createPoints();
    createEdges();
    createPolygons();
    return new Subject(points, edges, polygones);
};
