// конус
Surface.prototype.cone = (x = 0, y = 0, z = 0, r = 2, h = 5) => {
    const points = [];
    const edges = [];
    const polygones = [];

    function createPoints() {              
    }

    function createEdges() {
    }

    function createPolygons() {
    }

    createPoints();
    createEdges();
    createPolygons();
    return new Subject(points, edges, polygones);
};
